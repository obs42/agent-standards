# 共享规范仓库入口

使用中文。这里是通用规范与 Skill 的维护源，适用范围见 README.md。改变共享内容时考虑使用它的已有项目，不导入某个项目的专属路径、端口、账号配置或测试基准。

用户要求同步其他项目时，使用 skills/sync-agent-rules/SKILL.md；维护本仓库源文件时不反向运行同步覆盖自己。源文件清单见 manifest.json。运行同步脚本测试使用临时目录；提交和推送按用户实际授权执行。
